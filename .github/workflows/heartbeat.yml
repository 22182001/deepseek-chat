name: AI Heartbeat

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

jobs:
  trigger-heartbeat:
    runs-on: ubuntu-latest
    steps:
      - name: Call Vercel API
        run: |
          curl -s "https://deepseek-chat-bvg7u4kwj-22182001.vercel.app/api/keepalive"
