name: AI Heartbeat

on:
  schedule:
        - cron: '0 0-14 * * *'
  workflow_dispatch:

jobs:
  trigger-heartbeat:
    runs-on: ubuntu-latest
    steps:
      - name: Call Vercel API
        run: |
          curl -s "https://deepseek-chat-t7rn-6cmd41eif-22182001.vercel.app/api/keepalive"
